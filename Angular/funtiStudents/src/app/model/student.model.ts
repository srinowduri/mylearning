export class Student{
     constructor(
          public id: string, 
          public name: string, 
          public level: string, 
          public emailId: string, 
          public phoneNum: string
     ){}
}